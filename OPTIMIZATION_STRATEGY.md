# Homestead 최적화 전략

Homestead는 `Homestead.exe`, `data.pak`, 대표 저장 파일의 합계를 **1,474,560바이트(1.44MiB)** 이하로 맞춰야 한다. 일반적인 PC 게임에서는 편의상 넣을 수 있는 라이브러리나 원본 에셋도, 이 프로젝트에서는 용량과 시작 시간에 직접 영향을 준다.

그래서 이 프로젝트의 최적화는 단순히 “더 빠르게”가 아니라 다음 네 가지를 함께 다룬다.

1. 제출 파일 크기
2. 게임 실행 중의 메모리와 처리량
3. 시작·저장 시 사용자가 느끼는 대기 시간
4. 손상된 파일을 안전하게 거부하는 안정성

아래에는 실제 구현에 적용한 방식과, 검토했지만 최종 형식에는 넣지 않은 방식을 구분해 기록한다.

## 1. 먼저 범위를 줄이는 전략

가장 효과가 큰 최적화는 필요하지 않은 시스템을 만들지 않는 것이다.

- NPC, 대화, 계절, 전투, 낚시, 멀티플레이, 범용 스크립트 기능은 범위에서 제외했다.
- 농사, 상점, 판매, 일일 요청, 인벤토리, 저장, 작물 도감처럼 핵심 플레이 흐름을 우선했다.
- 동물은 구매·번식·생산물 시스템이 아니라, 정해진 경로를 움직이는 소수의 배경 동물로 한정했다.
- 동물은 저장하지 않는다. 따라서 동물 수가 늘어도 저장 파일 형식과 크기가 복잡해지지 않는다.

이 결정은 코드 크기와 테스트 범위를 줄이고, 각 기능을 더 안정적으로 다듬을 수 있게 한다.

## 2. 에셋 패키징: 원본 대신 필요한 조각만 담기

원본 PNG·WAV·텍스트 데이터는 개발용 폴더에만 둔다. Release 게임은 이런 파일을 읽지 않고, 빌드 도구가 만든 단일 `data.pak`만 읽는다.

### 2.1 스프라이트 선별

구매한 원본 에셋 묶음은 약 5.6MB이므로 그대로 제출할 수 없다. 에셋 패커는 다음만 포함한다.

- 실제 맵에서 쓰는 타일과 장식
- 플레이어의 필요한 방향·도구 프레임
- 여섯 작물의 성장 단계와 아이콘
- 선택한 UI 조각과 실제 문구에 쓰는 글꼴
- 닭과 개구리의 필요한 프레임

예를 들어 큰 동물 시트 전체나 사용하지 않는 가구, 사용하지 않는 글자는 런타임 패키지에 들어가지 않는다.

### 2.2 투명 여백 제거

스프라이트에는 실제 그림 주변에 투명한 픽셀이 많이 있을 수 있다. 패커는 투명한 바깥 여백을 잘라낸 뒤, 잘린 위치와 원래 크기를 함께 기록한다.

게임은 화면에 그릴 때 이 위치 정보를 다시 적용한다. 그래서 그림의 보이는 위치는 유지하면서도, 아틀라스에는 필요 없는 투명 픽셀을 저장하지 않는다.

### 2.3 중복 스프라이트 제거

서로 다른 이름으로 등록됐더라도 픽셀 내용과 원래 크기, 잘린 위치가 같은 스프라이트는 아틀라스에 한 번만 넣는다. 여러 이름은 같은 아틀라스 위치를 가리킨다.

이 방식은 아이콘·UI 조각·반복 타일처럼 같은 그림이 여러 번 쓰일 때 저장 공간을 줄인다.

### 2.4 팔레트 아틀라스

일반적인 RGBA 그림은 픽셀 하나에 색상 정보 4바이트를 사용한다. Homestead는 픽셀 아트의 색 수가 적다는 점을 이용해 다음처럼 저장한다.

```text
색상표: 실제 사용 색상 목록 (색상 하나당 4바이트)
픽셀: 색상 자체 대신 색상표 번호 1바이트
```

색상표는 최대 256색이며, 각 픽셀은 0~255 중 하나의 번호만 가진다. 시작할 때만 이 번호를 실제 RGBA 색으로 한 번 펼쳐 GPU 텍스처를 만든다. 게임 도중에는 보통의 텍스처처럼 처리하므로 화면을 그리는 코드는 단순하다.

### 2.5 하나의 큰 그림판으로 묶기

선별한 스프라이트는 작은 파일 여러 개가 아니라 하나의 아틀라스에 배치한다. 그러면 그림을 바꿀 때마다 별도 텍스처를 전환할 필요가 줄어든다.

- 파일 목록과 파일 열기 비용이 줄어든다.
- 화면에 그림을 묶어 전달하기 쉬워진다.
- 같은 그림판 안에서는 UV 좌표만 바꿔 여러 스프라이트를 그릴 수 있다.

## 3. RLE와 압축 방식의 선택

### 3.1 RLE란 무엇인가

RLE(Run-Length Encoding)는 같은 값이 연속되는 구간을 “값 + 반복 횟수”로 바꾸는 간단한 압축 방식이다.

```text
원본:  G G G G G G G G
RLE :  G, 8
```

잔디처럼 같은 타일이 길게 이어지는 맵, 같은 색이 길게 이어지는 그림에는 효과적일 수 있다.

### 3.2 Homestead에서의 판단

RLE는 맵 데이터 후보로 검토했다. 하지만 현재 맵은 작고, 맵을 시작할 때 한 번만 읽는다. 이 경우 RLE 해제 코드와 추가 검사 코드를 넣는 비용이 줄어드는 데이터보다 클 수 있다.

그래서 최종 패키지에는 **범용 RLE 해제기를 넣지 않았다.** 대신 아래 방식을 사용한다.

- 제작용 맵은 읽기 쉬운 문자 기반 형태로 작성한다.
- 빌드할 때 이 문자를 고정 크기 이진 타일 기록으로 바꾼다.
- 실행 중에는 PNG·CSV·문자 맵을 해석하지 않고, 바로 필요한 숫자를 읽는다.

현재 타일 하나는 다음처럼 6바이트의 고정 형식이다.

```text
바닥 그림 번호      2바이트
오브젝트 그림 번호  2바이트
상태 비트           1바이트
변형 번호           1바이트
```

이는 압축률만 보면 RLE보다 불리할 수 있지만, 맵이 작고 실행 중 빠르게 위치를 찾아야 하는 현재 게임에는 더 단순하고 안전하다. 맵이 커져 같은 타일이 훨씬 많이 반복되는 경우에는 RLE나 사전 방식 압축을 다시 측정해 적용할 수 있다.

## 4. 비트 단위 데이터 사용

“있다/없다”, “젖었다/안 젖었다”처럼 참·거짓 값이 여러 개 있을 때는 값마다 별도 변수를 두기보다 하나의 바이트 안에 여러 칸을 나눠 쓴다.

### 4.1 타일 상태 비트

타일의 상태는 하나의 8비트 값에 저장한다.

```text
비트 0: 통과 불가
비트 1: 물
비트 2: 경작됨
비트 3: 물을 줌
비트 4: 경작 가능 토양
```

예를 들어 “경작됨 + 물을 줌”은 비트 2와 3만 켠 값이다. 한 타일에 여러 상태를 담으면서도 저장 파일에는 1바이트만 추가하면 된다.

### 4.2 작물 도감 비트

도감은 여섯 작물의 해금 여부만 필요하다. 여섯 개의 `true/false` 값을 별도로 저장하지 않고, 1바이트의 여섯 비트를 사용한다.

```text
비트 0: 밀 해금
비트 1: 당근 해금
비트 2: 토마토 해금
비트 3: 감자 해금
비트 4: 옥수수 해금
비트 5: 양배추 해금
```

따라서 도감 전체 진행도는 저장 파일에서 1바이트면 충분하다. 읽을 때는 알려지지 않은 비트가 켜져 있지 않은지도 확인한다.

### 4.3 짧은 번호 사용

아이템, 작물, 맵, 품질, 입력 동작은 긴 문자열 대신 작은 정수 번호로 다룬다.

- 대부분의 종류 번호: 1바이트
- 골드·시간·개수처럼 더 큰 값: 2바이트
- 위치처럼 정밀도가 필요한 값: 필요한 범위의 정수

이 방법은 저장 파일과 메모리를 줄이고, 문자열 비교나 복잡한 객체 구조도 피하게 한다.

## 5. 저장: 전체 맵 대신 바뀐 부분만 기록

저장 파일은 정적 맵 전체를 복사하지 않는다. 처음부터 패키지에 들어 있는 농장·집 맵을 기준으로, 플레이어가 바꾼 부분만 저장한다.

저장하는 예시는 다음과 같다.

- 플레이어 위치와 현재 시간
- 선택한 퀵슬롯, 맵, 골드
- 인벤토리와 아이템 품질
- 경작·물 주기처럼 바뀐 타일 목록
- 심은 작물의 위치, 종류, 성장 단계, 물을 준 일수
- 일일 요청 완료 여부와 작물 도감 비트

저장하지 않는 예시는 다음과 같다.

- 원래 맵의 잔디·벽·나무 데이터
- 패키지에 이미 있는 작물 정의와 가격표
- 배경 동물의 현재 위치

플레이어 위치는 소수점 숫자 대신 논리 픽셀의 1/256 단위 정수로 저장한다. 같은 의미를 더 짧고 예측 가능한 형식으로 기록하기 위해서다.

### 5.1 저장 파일 검증

저장 파일은 16바이트 헤더와 본문으로 나뉜다. 헤더에는 식별 문자, 버전, 본문 크기, 체크섬이 있다.

불러올 때는 다음을 확인한다.

- 식별 문자와 버전이 맞는지
- 파일 길이와 기록된 본문 길이가 맞는지
- 체크섬이 맞는지
- 아이템·작물·맵 번호가 유효한지
- 타일·작물 개수가 최대치를 넘지 않는지
- 작물 성장 단계와 물을 준 일수가 서로 맞는지

새 저장은 먼저 임시 파일에 쓰고 닫은 다음 기존 파일과 교체한다. 이전 저장은 백업으로 보관한다. 저장 도중 문제가 생겨도 기존 저장을 바로 잃지 않기 위한 방법이다.

## 6. 오디오 압축과 재생

원본 WAV는 용량이 크다. 효과음은 패커에서 다음 조건의 작은 데이터로 바꾼다.

- 8kHz 샘플링
- 모노
- 2비트 ADPCM

ADPCM은 이전 소리 값과 작은 변화량만 기록하는 방식이다. 이 프로젝트의 2비트 형식은 한 바이트에 소리 샘플 4개를 담는다. 음질은 원본보다 낮아지지만, 짧은 농장 효과음에는 충분하고 파일 크기를 크게 줄인다.

게임 시작 시 선택한 배경 음악과 효과음을 한 번만 풀어 메모리에 둔다. 재생 중에는 고정된 소리 버퍼와 소수의 재생 채널을 재사용한다. 같은 효과음이 이미 재생 중이면 새 요청을 버려 소리가 과도하게 겹치는 것도 막는다.

오디오 장치가 없거나 초기화에 실패해도 게임은 멈추지 않고 무음으로 계속 실행한다.

## 7. 화면 처리와 게임 갱신

### 7.1 작은 기준 해상도

게임은 320×180 기준 화면에서 그린 뒤 정수 배율로 확대한다. 픽셀 사이를 부드럽게 섞지 않으므로, 작은 그림이 흐려지지 않는다. 남는 화면 영역은 검은 여백으로 처리한다.

### 7.2 필요한 것만 그리기

카메라 밖의 타일·작물·오브젝트는 렌더 명령에 넣지 않는다. 화면에 보이는 영역만 순회한다.

그림을 그릴 명령은 최대 개수가 정해진 배열에 쌓고, 레이어와 위치 순서로 정리한다. 게임 중 매 스프라이트마다 새 메모리를 만들지 않는다.

### 7.3 60회 기준 갱신

게임 규칙은 초당 60회 일정한 간격으로 갱신한다. 화면은 PC 성능에 따라 더 자주 또는 덜 자주 그릴 수 있지만, 이동·충돌·작물 성장 규칙은 같은 간격을 사용한다.

긴 멈춤 뒤에 너무 많은 갱신이 한꺼번에 몰리지 않도록, 한 프레임에서 처리할 횟수와 시간 누적량에도 상한을 둔다.

## 8. 시작 시간과 런타임 코드 줄이기

Release 게임은 원본 PNG, JSON, CSV, HLSL, WAV를 읽는 큰 해석기를 포함하지 않는다.

- 그림과 맵은 패커가 미리 변환한다.
- 셰이더는 빌드 중 미리 컴파일한다.
- 실행 중에는 작고 단순한 자체 이진 형식만 읽는다.
- 시작 화면은 로딩 작업이 끝날 때까지 진행 상태를 표시한다.

이 방식은 실행 파일 크기를 줄이고, 사용자가 검은 화면만 보는 시간을 줄인다.

## 9. 패키지와 데이터 검증

`data.pak`은 단순히 바이트를 이어 붙인 파일이 아니다. 패키지 전체와 각 항목에 크기·위치·체크섬 정보를 둔다.

읽을 때는 다음을 검사한다.

- 패키지 식별 문자와 버전
- 항목 수의 상한
- 항목 위치와 크기가 파일 범위 안에 있는지
- 항목 번호가 정렬되어 있고 중복되지 않는지
- 항목별 체크섬과 전체 체크섬
- 팔레트 색상 수, 스프라이트 사각형, 맵 기록의 범위

잘린 파일이나 잘못된 번호를 그대로 사용하지 않으므로, 손상된 데이터가 게임 상태를 망가뜨릴 가능성을 줄인다.

## 10. 빌드와 크기 측정

최적화는 “작아 보인다”는 감각으로 끝내지 않고, Release 결과를 매번 측정한다.

```text
Homestead.exe
+ data.pak
+ representative.sav
--------------------
  실제 제출 크기
```

최종 제출 폴더는 빌드 도구가 따로 만들고, 세 파일의 바이트 수와 SHA-256 해시를 기록한다. 한도를 넘으면 제출물 생성 자체가 실패한다.

현재 측정값은 다음과 같다.

| 파일 | 크기 |
|---|---:|
| `Homestead.exe` | 103,936B |
| `data.pak` | 366,132B |
| `representative.sav` | 173B |
| 합계 | **470,241B** |
| 제한까지 남은 공간 | **1,004,319B** |

## 11. 검증 전략

최적화로 파일이 작아져도 게임이 망가지면 의미가 없다. 그래서 다음을 함께 확인한다.

- Debug 빌드와 Release 빌드
- 저장·자산·입력·충돌·작물·상점 규칙을 확인하는 자동 테스트 29개
- 저장 파일과 패키지 파일의 잘린 데이터, 잘못된 버전, 잘못된 개수, 잘못된 번호 거부
- 실제 제출 폴더 생성과 크기 확인
- 직접 플레이로 화면 배치, 창 전환, 소리, 조작감 확인

## 요약

Homestead는 한 가지 큰 압축 기술에 의존하지 않는다. 필요한 기능과 에셋을 먼저 줄이고, 투명 여백 제거·중복 제거·팔레트 저장·작은 오디오 형식·비트 단위 상태·변경분만 저장하는 방법을 함께 사용한다.

RLE는 유용한 방법이지만 현재처럼 작은 맵에서는 해제 코드까지 포함한 전체 비용이 더 유리한지 측정해야 한다. 이 프로젝트는 실제 측정 결과를 우선해, 현재 형식에는 더 단순한 고정 크기 맵 기록을 선택했다.
