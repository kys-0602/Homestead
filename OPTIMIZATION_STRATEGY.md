# Homestead 최적화 전략

Homestead는 실행 파일, 게임 데이터, 대표 저장 파일을 모두 합쳐 **1,474,560바이트 이하**로 제출해야 합니다. 따라서 성능뿐 아니라 파일 크기와 안정성까지 함께 고려했습니다.

## 1. 필요한 기능부터 작게 만들기

가장 큰 전략은 처음부터 거대한 시스템을 만들지 않는 것입니다.

- NPC, 멀티플레이, 계절, 전투처럼 이번 게임에 꼭 필요하지 않은 기능은 넣지 않았습니다.
- 작물, 상점, 일일 요청, 도감처럼 농장 게임의 기본 재미를 만드는 기능에 집중했습니다.
- 동물은 구매·사육 시스템으로 확장하지 않고, 농장을 살아 있게 보이게 하는 가벼운 배경 요소로 만들었습니다.

기능 수를 무작정 늘리는 대신, 이미 있는 기능이 안정적으로 연결되도록 다듬는 방식을 선택했습니다.

## 2. 그림과 소리 용량 줄이기

원본 에셋은 게임에 그대로 넣지 않습니다. 게임에서 실제로 사용하는 부분만 골라 하나의 `data.pak` 파일로 정리합니다.

- 큰 그림 시트에서 필요한 타일과 애니메이션 프레임만 잘라 사용합니다.
- 투명한 여백은 가능한 한 줄이고, 같은 그림 조각은 한 번만 저장합니다.
- 색 정보를 줄인 아틀라스 형태로 그림을 묶어 파일 크기와 읽기 비용을 함께 줄입니다.
- 효과음은 짧고 작은 형식으로 준비하며, 배경 음악도 크기 예산 안에서 사용합니다.
- 글꼴도 실제 화면에 쓰는 영문자·숫자·기호만 포함합니다.

이 덕분에 원본 에셋 묶음 전체를 넣지 않아도 필요한 화면을 구성할 수 있습니다.

## 3. 화면을 그리는 비용 줄이기

픽셀 아트 게임은 같은 그림을 많이, 자주 그립니다. 그래서 그리는 순서를 단순하게 유지했습니다.

- 게임 화면은 작은 기준 해상도에서 그린 뒤 정수 배율로 확대합니다.
- 그림은 가까운 픽셀을 그대로 확대해 흐려지지 않게 합니다.
- 화면에 보이는 맵 타일과 오브젝트만 그립니다.
- 여러 그림을 가능한 한 묶어서 화면에 전달합니다.
- 플레이어, 나무, 동물은 발 위치를 기준으로 앞뒤 순서를 정해 자연스럽게 겹치도록 합니다.

게임 규칙과 화면 그리기를 분리해, 화면을 그리는 코드가 농장 상태를 바꾸지 않도록 했습니다.

## 4. 게임 상태를 단순하게 유지하기

매 프레임마다 많은 메모리를 만들고 버리면 게임이 불안정해질 수 있습니다. 그래서 자주 쓰는 데이터는 미리 정한 작은 배열에 보관합니다.

- 인벤토리, 작물, 동물, 화면에 그릴 명령은 최대 개수를 정해 둡니다.
- 작물과 동물은 필요한 정보만 가진 단순한 구조로 관리합니다.
- 동물은 저장하지 않는 배경 요소이므로 저장 파일 크기를 늘리지 않습니다.
- 충돌은 전체 맵을 검사하지 않고, 플레이어 주변 타일과 동물만 확인합니다.

이 방식은 작은 게임에 알맞고, 메모리 사용량과 처리 시간을 예측하기 쉽습니다.

## 5. 저장 파일을 작고 안전하게 만들기

저장 파일에는 맵 전체를 넣지 않습니다. 처음 맵과 달라진 내용만 기록합니다.

- 플레이어 위치, 시간, 골드, 인벤토리, 심은 작물, 갈아 둔 땅처럼 필요한 정보만 저장합니다.
- 작물 이름이나 그림 자체가 아니라 짧은 고정 번호를 사용합니다.
- 파일 형식에 버전을 넣어 이전 저장 파일도 가능한 한 불러올 수 있게 했습니다.
- 저장 전후에 길이, 값의 범위, 체크섬을 확인해 잘못된 파일을 거부합니다.
- 새 저장은 임시 파일에 먼저 기록한 뒤 교체하고, 이전 저장은 백업으로 남깁니다.

## 6. 작업마다 측정하고 검증하기

최적화는 추측보다 측정이 중요합니다. 기능을 추가한 뒤에는 다음을 확인했습니다.

- Debug 빌드와 관련 자동 테스트가 통과하는지 확인합니다.
- Release 빌드에서 실행 파일과 `data.pak` 크기를 측정합니다.
- 대표 저장 파일을 포함한 실제 제출 크기를 계산합니다.
- 저장 파일, 자산 파일, 입력, 충돌, 작물 규칙을 자동 테스트로 확인합니다.
- 화면 배치, 소리, 창 전환처럼 자동으로 확인하기 어려운 부분은 직접 플레이하며 점검합니다.

현재 최종 제출물의 합계는 **470,241바이트**로, 제한보다 **1,004,319바이트** 작습니다.

## 정리

Homestead의 최적화는 복잡한 기술보다 다음 세 가지에 집중합니다.

1. 꼭 필요한 기능만 만든다.
2. 실제로 쓰는 그림·소리·저장 정보만 담는다.
3. 기능을 추가할 때마다 빌드, 테스트, 파일 크기를 확인한다.

이 전략 덕분에 작은 제출 용량 안에서도 농사, 경제, 저장, UI, 오디오, 배경 동물이 연결된 게임 흐름을 유지할 수 있습니다.
