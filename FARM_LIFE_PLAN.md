# 농장 생활 콘텐츠 구현 계획

## 1. 목적과 범위

농장의 모든 잔디를 경작할 수 있는 현재 규칙을 고쳐 플레이어가 경작 가능 구역을
즉시 알 수 있게 한다. 그 위에 장기 보상이나 업적 시스템 없이 작물 도감과 배경
동물을 추가해 농장이 살아 있는 장소처럼 느껴지게 한다.

이번 범위에는 농지 확장, 동물 구매·사육·생산물, 동물 충돌, 업적 보상, NPC, 장식
재배치는 넣지 않는다. 동물은 순수한 배경 연출이며 게임 규칙이나 저장 진행을 바꾸지
않는다.

## 2. 경작 가능 지형

1. `TileFlag`에 정적 `Farmable` 속성을 추가한다.
2. 농장 맵 문법에 경작 가능 토양 전용 기호를 추가하고, 맵에는 한 개의 연속된 농지
   구역만 지정한다. 길·집 주변 잔디·장식 주변은 경작 불가로 남긴다.
3. 전용 토양에는 기존 잔디와 구분되는 절제된 흙 바닥 타일을 사용한다. 실제로 괭이를
   사용하면 현재의 연결형 경작지 타일과 물 뿌린 오버레이가 그 위에 그려진다.
4. `CanTill`은 `Farmable` 타일에서만 성공하게 한다. 물 주기·심기·성장·수확과 저장된
   경작 타일은 기존 규칙을 유지한다.
5. 기존 저장 파일에서 이미 경작된 타일은 계속 불러오되, 새로 괭이를 쓸 수 있는 곳만
   제한한다. 이로써 기존 저장을 손상시키지 않는다.

## 3. 작물 도감

### 플레이 흐름

1. 집 안 책장 앞에서 `E`를 누르면 도감 UI가 열린다.
2. 도감은 여섯 작물을 두 열로 표시한다.
3. 한 번이라도 수확한 작물은 이름, 성장 일수, 기본 판매가와 은·금 품질 판매 배율을
   보여 준다. 아직 수확하지 않은 작물은 `UNSEEN`으로 표시한다.
4. `ESC` 또는 다시 `E`를 누르면 닫힌다. 열린 동안 이동·도구·시장·인벤토리 입력은
   막는다.

### 데이터와 저장

- `CropCatalogue`는 작물 ID별 비트 집합만 보관한다. 수확 성공 시 해당 비트를 켠다.
- 보상·점수·업적은 만들지 않는다.
- save version 6에 도감 비트를 넣고, version 1~5 저장은 빈 도감으로 이관한다.
- 수확물 자체의 품질 및 기존 save v5 품질 이관 규칙은 유지한다.

## 4. 배경 동물

- `Animals/Chicken/Chicken_01.png`에서 걷기·대기 프레임만 잘라 2~3마리의 닭을
  농장 맵에 추가한다. 소·양 등 큰 동물과 전체 원본 시트는 패키지에 넣지 않는다.
- 닭은 지정된 농장 가장자리 안전 구역 안에서 짧게 걷고, 잠시 대기한 뒤 방향을
  바꾼다. 결정적 고정 난수를 사용해 프레임률·저장 불러오기와 무관하게 동작한다.
- 닭은 플레이어, 나무, 장식, 경작지와 충돌하지 않으며 도구·수확·저장 대상도 아니다.
- 별도 ECS 확장 대신 `Application`이 소유하는 작은 고정 배열과 전용 업데이트·렌더
  함수로 구현한다. 렌더는 발 위치 Y 정렬을 사용한다.
- 농장 밖과 집 안에서는 업데이트·렌더하지 않는다.

## 5. 구현 순서

1. 경작 가능 토양 플래그, 맵 문법, 도구 제한, 타일 렌더와 테스트
2. 도감 데이터, 수확 연결, 책장 상호작용, UI, save v6 이관과 테스트
3. 닭 스프라이트 패킹, 고정 배열 배회, 렌더, 비충돌 검증
4. Debug/Release 빌드, 전체 테스트, 패키지 크기 측정 및 수동 게임플레이 검증

## 6. 검증 기준

- 경작 가능 구역에서만 괭이·심기·물 주기가 가능하고, 나머지 잔디에서는 괭이가
  실패한다.
- 모든 여섯 작물을 수확하면 도감의 항목이 각각 해제되고 저장 후에도 유지된다.
- 기존 version 1~5 저장이 정상적으로 불러와지며, 비정상 도감 비트·잘린 저장은
  거부된다.
- 닭이 농장 밖·집 안·경작지로 들어가지 않고 플레이어 이동을 방해하지 않는다.
- 닭의 대기·걷기 애니메이션, 깊이 정렬, 창/전체 화면, 저장 후 재실행을 수동으로
  확인한다.
- Release `Homestead.exe`, `data.pak`, 대표 save 합계가 1,474,560 bytes 이하인지
  기록한다.
