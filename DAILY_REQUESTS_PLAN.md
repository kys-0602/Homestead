# 일일 의뢰 게시판 구현 계획

## 1. 목적

현재의 `씨앗 구매 → 재배 → 수확 → 판매` 반복에 매일 달라지는 단기 목표와
추가 보상을 제공한다. 별도 NPC, 대화 트리, 범용 퀘스트 VM은 추가하지 않고 기존
농장 표지판과 작물·인벤토리·경제·날짜 시스템을 재사용한다.

## 2. 플레이 흐름

1. 날짜마다 밀, 당근, 토마토 중 하나를 요구하는 의뢰가 하나 정해진다.
2. 농장 표지판 앞에서 `E`를 누르면 게시판 UI가 열린다.
3. UI는 요구 작물, 요구 수량, 현재 보유량, 골드 보상을 표시한다.
4. 수량이 충분할 때 `E` 또는 확인 버튼을 누르면 작물을 차감하고 골드를 지급한다.
5. 같은 날짜의 의뢰는 한 번만 완료할 수 있다.
6. 침대에서 다음 날로 넘어가면 새 의뢰가 활성화된다.

`ESC`는 게시판을 닫으며, 게시판이 열린 동안 이동·도구·시장·인벤토리 입력은
차단한다. 마우스 확인 버튼과 키보드 조작은 같은 활성화 경로를 사용한다.

## 3. 의뢰 생성과 밸런스

- 의뢰는 저장된 `day`만으로 결정되는 고정 의사난수 결과를 사용한다.
- 동일한 날짜와 save에서는 항상 같은 의뢰가 나와 재실행으로 결과를 바꿀 수 없다.
- 작물 선택은 세 작물이 장기적으로 고르게 등장하도록 날짜 기반 순환에 작은
  permutation을 적용한다.
- 요구 수량은 작물별 재배 기간과 판매가를 고려한 작은 정적 표로 제한한다.

초기 밸런스 후보:

| 작물 | 요구 수량 | 기본 판매 가치 | 의뢰 보상 |
|---|---:|---:|---:|
| 밀 | 3~5 | 21~35G | 28~46G |
| 당근 | 2~4 | 24~48G | 32~62G |
| 토마토 | 1~3 | 20~60G | 28~76G |

정확한 수치는 `DailyRequestDefinition` 정적 테이블 한 곳에서 관리한다. 의뢰 보상은
같은 작물을 시장에 판매하는 것보다 약 25~35% 높게 설정하되 `uint16_t` 골드 상한을
넘는 납품은 거부한다. 납품은 전체 요구 수량을 한 번에 처리하며 부분 납품은 없다.

## 4. 런타임 구조

새로운 범용 퀘스트 계층을 만들지 않고 다음의 작은 값 타입과 함수로 구현한다.

```text
DailyRequestDefinition { crop, item, minimumCount, countRange, bonusPercent }
DailyRequestState      { completed }

BuildDailyRequest(day) -> crop, requiredCount, reward
CanCompleteRequest(request, inventory, gold)
CompleteRequest(request, state, inventory, gold)
```

- 정적 정의와 완료 규칙은 `Game/DailyRequest`가 소유한다.
- `Application`은 게시판 열림 여부와 UI focus를 소유하고 입력을 조정한다.
- `DailyRequestUI`는 읽기 전용 요청 정보로 render command만 생성한다.
- 작물 수확 이벤트를 누적하지 않는다. 납품 시점의 실제 inventory 수량을 검사한다.
- 동적 할당, 문자열 조립용 스트림, 범용 이벤트/스크립트 시스템을 추가하지 않는다.

## 5. 표지판 상호작용과 UI

- 기존 `TileGraphic::Sign`을 의뢰 게시판으로 사용하며 새 월드 에셋은 추가하지 않는다.
- 키보드 상호작용 시 주변 특수 오브젝트 탐색에 표지판을 포함한다.
- 게시판 패널은 기존 9-slice UI와 bitmap font를 재사용한다.
- 표시 문구는 `REQUEST`, 작물명, `NEED n`, `HAVE n`, `REWARD nG`, 상태 문구로 제한한다.
- 상태 문구는 `DELIVER`, `NEED MORE`, `COMPLETED` 중 하나다.
- 완료 성공 시 기존 UI 확인 효과음을 재사용하고 짧은 HUD 알림을 표시한다.
- 새로운 글리프가 필요하면 실제 문구에 필요한 것만 atlas에 추가한다.

## 6. 날짜와 저장 규칙

- 현재 날짜가 요청 정의의 ID 역할을 하므로 crop/count/reward를 save에 중복 저장하지 않는다.
- save에는 현재 날짜 의뢰의 `completed` 플래그만 추가한다.
- save format version을 올리고 이전 version은 `completed = false`로 이관한다.
- decode 시 boolean 범위와 기존 모든 필드를 계속 검증한다.
- 침대에서 다음 날로 전환할 때 `completed`를 false로 초기화한 뒤 기존 침대 저장
  경로에 포함한다.
- 게시판 완료 후 침대에 눕지 않고 종료하면 기존 정책대로 그날의 납품도 저장되지 않는다.

## 7. 구현 순서

1. `DailyRequest` 값 타입, 날짜 기반 생성, 납품 규칙과 단위 테스트
2. save version 갱신, 완료 플래그 encode/decode, 구버전 migration 및 malformed 테스트
3. 농장 표지판 상호작용과 게시판 open/close 입력 흐름
4. 키보드·마우스 공용 활성화가 가능한 `DailyRequestUI`
5. 다음 날 초기화, 완료 알림, 시장·인벤토리·pause와의 상호 배제
6. Debug/Release 전체 테스트, 실제 GUI 수동 검증, 제출 크기 측정

## 8. 자동 검증 기준

- 같은 날짜는 같은 요청을 만들고 여러 날짜에 세 작물이 모두 등장한다.
- 수량 부족, 이미 완료, 골드 overflow에서는 inventory와 gold가 변하지 않는다.
- 성공 시 요구 수량만 정확히 차감하고 보상을 한 번만 지급한다.
- save round-trip에서 완료 상태가 유지되고 구버전 save는 미완료 상태로 이관된다.
- 손상된 완료 플래그, 잘못된 version, truncated save를 거부한다.
- 게시판 hit-test 경계와 키보드/마우스 focus 경로가 일치한다.
- 게시판이 열린 동안 월드 입력이 실행되지 않는다.
- 다음 날에는 이전 완료 상태가 초기화되고 다른 날짜의 요청이 표시된다.

## 9. 수동 검증 기준

- 표지판 앞 `E`와 우클릭으로 게시판이 열리는지 확인
- 수량 부족, 납품 가능, 완료 후 세 상태의 문구와 버튼이 자연스러운지 확인
- 납품 후 inventory와 gold가 정확히 변하고 중복 보상이 불가능한지 확인
- 게시판에서 방향키, `E`, 마우스, `ESC`가 모두 정상 동작하는지 확인
- 게시판을 연 상태에서 이동·도구·시장·인벤토리가 작동하지 않는지 확인
- 침대 저장 후 재실행하면 완료 상태가 유지되는지 확인
- 다음 날 새 의뢰가 나오며 농장 표지판 외 오브젝트 동작이 변하지 않는지 확인
- 320×180, 창 모드, 전체 화면에서 문구가 패널 밖으로 벗어나지 않는지 확인

## 10. 크기 목표와 제외 범위

- 새 이미지·오디오 없이 기존 에셋을 재사용한다.
- Release EXE 증가 목표는 8KiB 이하, `data.pak` 증가는 font/string metadata를 포함해
  2KiB 이하, save 증가는 4바이트 이하로 둔다.
- NPC, 여러 동시 의뢰, 부분 납품, 연속 의뢰 보너스, 시간 제한, 스토리 대화,
  범용 퀘스트 VM은 이번 범위에서 제외한다.

## 11. 완료 조건

- 하루 1개 의뢰의 생성·확인·납품·보상·다음 날 갱신이 완결된다.
- 키보드와 마우스 양쪽으로 전체 흐름을 진행할 수 있다.
- 기존 침대 전용 저장 정책과 구버전 save 호환성이 유지된다.
- 관련 단위 테스트와 Debug/Release 빌드가 통과한다.
- Release 제출물 크기와 잔여 예산을 `DEVELOPMENT_PROGRESS.md`에 기록한다.
- 남은 GUI/D3D11 수동 검증 항목을 명시한다.
